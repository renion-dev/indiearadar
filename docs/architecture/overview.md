# Архітектура Indie AI Radar

## Загальна схема

┌─────────────────────────────────────────────────────────────┐
│ DATA LAYER │
├─────────────────────────────────────────────────────────────┤
│ _data/seed/ _data/programmatic/ _tools/ │
│ - professions.yml - best-tools/.yml - tool/.md │
│ - industries.yml - alternatives/.yml │
│ - pricing.yml - comparisons/.yml │
│ - platforms.yml │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ GENERATION LAYER │
├─────────────────────────────────────────────────────────────┤
│ seed_generator.py programmatic_generator.py │
│ Seed → YAML Definitions YAML → Markdown Pages │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ PRESENTATION LAYER │
├─────────────────────────────────────────────────────────────┤
│ pages/programmatic/*.md Jekyll │
│ - best-ai-tools-for-developers.md → Static Site │
│ - cursor-alternatives.md │
└─────────────────────────────────────────────────────────────┘
text


## Ключові рішення

1. **Seed — єдине джерело істини** (ADR-0008)
2. **Генератори стабільні** (ADR-0009)
3. **Data-first, а не code-first** (METADATA_STRATEGY.md)
4. **YAML-таксономія** — структура каталогів замість одного великого файлу

## Майбутні напрямки

- Розширення seed-даних (індустрії, юзкейси, ціни)
- AI-збагачення контенту (FAQ, описи)
- Внутрішня перелінковка
- JSON-LD
